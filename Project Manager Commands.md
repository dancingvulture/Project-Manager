# General
| Command                       | Alias                  | Help                                                                                                                        | Notes                                                                                      |
| ----------------------------- | ---------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `help {args}`                 | `? {args}`             | Display a general help message (no args), or pass a command or other term as an argument to display its help text.          | -                                                                                          |
| `welcome`                     | `welcome`              | Display the welcome message.                                                                                                | -                                                                                          |
| `exit`                        | `ex`                   | Exit the project manager.                                                                                                   | -                                                                                          |
| `cd {path}`                   | `cd {path}`            | Change the directory. You cannot leave the project or test directory.                                                       | -                                                                                          |
| `view working directory`      | `vw`                   | See the working directory's full path.                                                                                      | -                                                                                          |
| `subprocess {args}`           | `sp {args}`            | Pass this command directly into the subprocessor.                                                                           | -                                                                                          |
| `new alias {command} {alias}` | `ca {command} {alias}` | Create a new alias that exists only within this project. <br>'command' is the full command and 'alias' is the shortcut.<br> | - If an alias would overwrite an existing one, there should be a warning and (Y/N) prompt. |

# Python
| Command                           | Alias         | Help                                                              | Notes                                                                     |
| --------------------------------- | ------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------- |
| `python3`                         | `py`          | Use the python interpreter.                                       | Only needed on ubuntu.                                                    |
| `py main.py {args}`               | `run {args}`  | Run 'main.py', adding arguments as normal (if any).               | -                                                                         |
| `ipython`                         | `ipy`         | Launch ipython.                                                   | -                                                                         |
| `py -m venv .venv`                | `mkvenv`      | Create a virtual venv.                                            | -                                                                         |
| `activate venv`                   | `venv`        | Activate local venv.                                              | - Not a system command.<br>- Venv to activate will be platform dependant. |
| `py -m pip install {module}`      | `pi {module}` | Install a module with pip (will add to 'requirements.txt')        | - Not a system command.<br>- Alters `requirements.txt`                    |
| `py -m pip uninstall {module}`    | `pu {module}` | Uninstall a module with pip (will remove from 'requirements.txt') | - Not a system command.<br>- Alters `requirements.txt`                    |
| `py -m pip install --upgrade pip` | `pg`          | Upgrade pip to the latest version.                                | -                                                                         |
| `py -m pip list`                  | `pl`          | List all modules pip has installed.                               | -                                                                         |
| `pip install -r requirements.txt` | `pir`         | Install all modules in the `requirements.txt` file.               | -                                                                         |

# Git
| Command                                                                                                     | Alias                    | Help                                                                                                     | Notes                                                                                                                        |
| ----------------------------------------------------------------------------------------------------------- | ------------------------ | -------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| `git init`                                                                                                  | `gi`                     | Initialize git.                                                                                          | -                                                                                                                            |
| `git log`                                                                                                   | `gl`                     | View git log.                                                                                            | -                                                                                                                            |
| `git add`                                                                                                   | `ga {directory}`         | Add folder or file to git (`.` adds everything rooted in the working directory).                         | -                                                                                                                            |
| `git commit -m {comment}`                                                                                   | `gc {comment}`           | Commit changes to current branch.                                                                        | -                                                                                                                            |
| `git commit -F changes.txt`                                                                                 | `gcf`                    | Commit changes to current branch, using changelog as comment.                                            | - Clear 'changes.txt' after this.<br>- Not a system command.                                                                 |
| `git remote add origin {url}`                                                                               | `grao {url}`             | Add remote origin.                                                                                       | -                                                                                                                            |
| `git remote -v`                                                                                             | `grv`                    | View remote branch URL.                                                                                  | -                                                                                                                            |
| `git fetch origin`                                                                                          | `gfo`                    | Fetch from origin (add without merging local files).                                                     | -                                                                                                                            |
| `git pull origin`                                                                                           | `gplo`                   | Pull from origin (add while merging local files).                                                        | -                                                                                                                            |
| `git push origin {branch}`                                                                                  | `gpso {branch}`          | Push branch to origin.                                                                                   | -                                                                                                                            |
| `git checkout {branch}`                                                                                     | `gch {branch}`           | Switch to existing branch.                                                                               | -                                                                                                                            |
| `git checkout -b {branch}`                                                                                  | `gnch {branch}`          | Make new branch and switch to it.                                                                        | -                                                                                                                            |
| `gi &&` <br>`grao {url} &&` <br>`gfo &&` <br>`git checkout origin/{branch} &&` <br>`git switch -c {branch}` | `gfclone {url} {branch}` | Create a git instance and populate it with the contents of a remote branch.                              | - Not a system command.<br>- `&&` only works with bash, will need different syntax for powershell.                           |
| `gnch {branch} &&`<br>`ga . &&`<br>`gcf` or `gc {comment}`                                                  | `gnb {branch} {comment}` | Add all changes and commit to a new branch. If no comment is entered will default to using `changes.txt` | - Not a system command.                                                                                                      |
| `git rm -r --cached &&`<br>`ga . &&`<br>`gc "Removing unused files"`                                        | `grmu`                   | Remove files stuck in the commit, which have since been added t '.gitignore'                             | -                                                                                                                            |
| `git ignore pycache`                                                                                        | `gip`                    | Recursively add all '\_\_pycache\_\_' directories to '.gitignore'.                                       | - Not a system command.<br>- This could be run every time git add is used, to avoid ever accidentially adding pycache files. |

# Project
None of these will be system commands.

| Command                                                                     | Alias                                                           | Help                                                                                                                                                                                                                 | Notes                                                                                                                                                                                                                         |
| --------------------------------------------------------------------------- | --------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `new project`<br>`-d {desc}` <br>`-rb {url,branch}`<br>`{project name}`<br> | `np `<br>`-d {desc}` <br>`-rb {url,branch}`<br>`{project name}` | Creates the a folder in the projects folder based off of the new project template. <br>Optionally add a description that will be added to the README.<br>Optionally make the new project a clone of a remote branch. | - Create project directory with project template. <br>- Create license. <br>- Add description to readme if provided. <br>- Make venv. <br>- Activate venv. <br>- Upgrade pip. <br>- Install ipython. <br>- Initiate git repo. |
| `new test -tp {test name}`                                                  | `nt {test name}`                                                | Creates a new test in the test directory.                                                                                                                                                                            | - Create test directory.<br>- Make venv.<br>- Activate venv.<br>- Upgrade pip.<br>- Install ipython.                                                                                                                          |
| `open project {project name}`                                               | `op {project name}`                                             | Open an existing project.                                                                                                                                                                                            | Changes working directory and input prompt.                                                                                                                                                                                   |
| `open test {test name}`                                                     | `ot {test name}`                                                | Open an existing test.                                                                                                                                                                                               | Changes working directory and input prompt.                                                                                                                                                                                   |
| `log change {change}`                                                       | `lc {change}`                                                   | Log a change, adding a bullet point to the comment for the next commit.                                                                                                                                              | -                                                                                                                                                                                                                             |
| `view log`                                                                  | `vl`                                                            | View the current commit's comment.                                                                                                                                                                                   | -                                                                                                                                                                                                                             |
| `view projects`                                                             | `vp`                                                            | View all projects currently in the projects folder.                                                                                                                                                                  | -                                                                                                                                                                                                                             |
| `view tests`                                                                | `vt`                                                            | View all tests currently in the tests folder.                                                                                                                                                                        | -                                                                                                                                                                                                                             |

